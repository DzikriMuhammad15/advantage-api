class AdvertisingContent {
  constructor(userId, name, category, imageUrl, status) {
    this.userId = userId;
    this.name = name;
    this.category = category;
    this.imageUrl = imageUrl;
    this.status = status;
  }
}

module.exports = AdvertisingContent;
